# typed: false
# frozen_string_literal: true

# This file was generated by minijinja.
class poof < Formula
  desc "The AI coding agent built for the terminal."
  homepage "https://github.com/pirafrank/poof"
  version "{{ version }}"

  on_macos do
    if Hardware::CPU.intel?
      url "https://github.com/pirafrank/poof/releases/download/v{{ version }}/poof-{{ version }}-x86_64-apple-darwin.tar.gz"
      sha256 "{{ sha256_darwin_x86_64 }}"

      def install
        bin.install "poof"
      end
    end
    if Hardware::CPU.arm?
      url "https://github.com/pirafrank/poof/releases/download/v{{ version }}/poof-{{ version }}-aarch64-apple-darwin.tar.gz"
      sha256 "{{ sha256_darwin_aarch64 }}"

      def install
        bin.install "poof"
      end
    end
  end

  on_linux do
    if Hardware::CPU.intel? and Hardware::CPU.is_64_bit?
      url "https://github.com/pirafrank/poof/releases/download/v{{ version }}/poof-{{ version }}-x86_64-unknown-linux-gnu.tar.gz"
      sha256 "{{ sha256_linux_x86_64 }}"
      def install
        bin.install "poof"
      end
    end
    if Hardware::CPU.arm? and Hardware::CPU.is_64_bit?
      url "https://github.com/pirafrank/poof/releases/download/v{{ version }}/poof-{{ version }}-aarch64-unknown-linux-gnu.tar.gz"
      sha256 "{{ sha256_linux_aarch64 }}"
      def install
        bin.install "poof"
      end
    end
  end
end
