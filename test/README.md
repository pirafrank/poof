# Devcontainer feature tests

Smoke tests for the `poof` devcontainer feature.

The directory name and location follow the [devcontainer features test specification](https://containers.dev/implementors/features-distribution/#testing-features):
tests must live at `test/<feature-id>/` relative to the repository root to be
automatically discovered by `devcontainer features test`.
